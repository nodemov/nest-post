### Variables
@baseUrl = http://localhost:3000
@postId = 18

### Create a new post
POST {{baseUrl}}/posts
Content-Type: application/json

{
  "title": "My First Post",
  "detail": "This is the content of my first post. It contains useful information.",
  "cover": "https://example.com/images/cover1.jpg"
}

### Create a post without cover
POST {{baseUrl}}/posts
Content-Type: application/json

{
  "title": "Post Without Cover",
  "detail": "This post doesn't have a cover image"
}

### Get all active posts (excluding soft-deleted)
GET {{baseUrl}}/posts

### Get paginated posts - page 1 with default limit (10 items)
GET {{baseUrl}}/posts?page=1

### Get paginated posts - page 2 with 20 items per page
GET {{baseUrl}}/posts?page=2&limit=20

### Get paginated posts - page 5 with 50 items per page
GET {{baseUrl}}/posts?page=5&limit=50

### Get all posts including soft-deleted
GET {{baseUrl}}/posts/all/with-deleted

### Get all posts including soft-deleted (paginated)
GET {{baseUrl}}/posts/all/with-deleted?page=1&limit=15

### Get only deleted posts
GET {{baseUrl}}/posts/deleted/only

### Get only deleted posts (paginated)
GET {{baseUrl}}/posts/deleted/only?page=1&limit=10

### Get a specific post by ID
GET {{baseUrl}}/posts/{{postId}}

### Update a post
PATCH {{baseUrl}}/posts/{{postId}}
Content-Type: application/json

{
  "title": "Updated Post Title"
}

### Update post with multiple fields
PATCH {{baseUrl}}/posts/{{postId}}
Content-Type: application/json

{
  "title": "Completely Updated Post",
  "detail": "Updated detail content",
  "cover": "https://example.com/images/new-cover.jpg"
}

### Soft delete a post
DELETE {{baseUrl}}/posts/{{postId}}

### Restore a soft-deleted post
PATCH {{baseUrl}}/posts/{{postId}}/restore

### Force delete (permanently delete) a post
DELETE {{baseUrl}}/posts/{{postId}}/force

### Test validation - missing title (should return 400)
POST {{baseUrl}}/posts
Content-Type: application/json

{
  "detail": "Post without title"
}

### Test validation - missing detail (should return 400)
POST {{baseUrl}}/posts
Content-Type: application/json

{
  "title": "Post without detail"
}

### Test validation - extra fields (should return 400)
POST {{baseUrl}}/posts
Content-Type: application/json

{
  "title": "Test Post",
  "detail": "Test Detail",
  "extraField": "This should not be allowed"
}

### Get non-existent post (should return 404)
GET {{baseUrl}}/posts/9999

### Test validation - invalid ID format (should return 400)
GET {{baseUrl}}/posts/abc

### Test validation - invalid ID format with special chars (should return 400)
GET {{baseUrl}}/posts/12@34

### Test validation - negative ID (should return 400)
GET {{baseUrl}}/posts/-5

### Test validation - decimal ID (should return 400)
GET {{baseUrl}}/posts/12.5
